FROM python:3.8.12-slim-buster

USER root

RUN apt-get update && apt-get install librdkafka-dev python3-dev -y

WORKDIR /src

COPY requirements.txt .

RUN pip3 install -r requirements.txt

COPY streams /src

ENTRYPOINT ["python3"]